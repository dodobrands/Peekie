{
  "eval_id": 1,
  "eval_name": "attachments-from-failures",
  "prompt": "ok so one of our snapshot tests is doing something weird — I need the screenshots and any other attached debug files from just the failing tests. The xcresult is at `Tests/PeekieTests/Resources/SPM-26.5-iOS.xcresult`. Pull only the failure attachments into `/tmp/peekie-eval-failed-attachments/` and tell me what landed there.",
  "assertions": [
    {"name": "files-landed-in-requested-directory", "description": "The target directory exists and contains at least one attachment file."},
    {"name": "only-failure-attachments", "description": "Only attachments associated with failing tests landed."},
    {"name": "tells-user-what-landed", "description": "Answer enumerates files (count + names or directory listing)."},
    {"name": "no-non-failure-attachments-leaked", "description": "No attachments from passing/success/skipped tests in the directory."}
  ]
}
